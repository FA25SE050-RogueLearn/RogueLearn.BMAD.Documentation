' remove-skill-mapping-seq.puml
@startuml
actor Admin
participant AdminManagementUI as UI
participant ":SubjectsController" as Controller
participant "RemoveMapping\nHandler" as Handler
participant "MappingRepository" as DB

Admin -> UI : Click "Remove" on Skill Mapping
activate UI

UI -> Controller : DELETE /subjects/{sid}/skills/{kid}
activate Controller

Controller -> Handler : Handle(command)

activate Handler
    Handler -> DB : FirstOrDefaultAsync(sid, kid)
    
    alt Not Found
        Handler --> Controller : Throw NotFoundException
    else Found
        Handler -> DB : DeleteAsync(id)
        Handler --> Controller : Return Success
    end
deactivate Handler

Controller --> UI : 204 No Content
deactivate Controller

UI -> UI : Remove Item from List
UI --> Admin : Show "Mapping Removed" Success
deactivate UI
@enduml